cmake_minimum_required(VERSION 3.13)
project(kakehashi_anki_layout LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 20)
if(NOT KAKEHASHI_REPO_ROOT)
  message(FATAL_ERROR "KAKEHASHI_REPO_ROOT is required")
endif()
add_subdirectory("${KAKEHASHI_REPO_ROOT}/node_modules/react-native/ReactCommon/yoga/yoga" yoga)
add_executable(anki-layout layout.cpp)
target_include_directories(anki-layout PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
target_link_libraries(anki-layout PRIVATE yogacore)
