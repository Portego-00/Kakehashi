diff --git a/node_modules/piper-plus/src/index.js b/node_modules/piper-plus/src/index.js
index 885d291..768c7be 100644
--- a/node_modules/piper-plus/src/index.js
+++ b/node_modules/piper-plus/src/index.js
@@ -595,18 +595,24 @@ export class PiperPlus {
 
       progress({ stage: 'model', progress: 0, message: 'Resolving model...' });
 
-      const modelManager = new ModelManager();
-      const { modelUrl, configUrl, configFallbackUrl } = await modelManager.resolveUrls(options.model);
-
-      progress({ stage: 'model', progress: 0.1, message: 'Downloading config...' });
-      let configResponse = await fetch(configUrl);
-      if (!configResponse.ok && configResponse.status === 404 && configFallbackUrl) {
-        configResponse = await fetch(configFallbackUrl);
-      }
-      if (!configResponse.ok) {
-        throw new Error(`Failed to fetch config: ${configResponse.status} ${configResponse.statusText}`);
-      }
-      this._config = await configResponse.json();
+      const modelManager = new ModelManager({
+        verifyOnCacheHit: true,
+        expectedSha256: options.modelSha256,
+        expectedByteLength: options.modelByteLength,
+      });
+      const { modelData, config } = await modelManager.loadModel(options.model, {
+        onProgress: ({ loaded, total, percentage }) => {
+          const size = total > 0
+            ? `${(loaded / 1024 / 1024).toFixed(1)} of ${(total / 1024 / 1024).toFixed(1)} MB`
+            : `${(loaded / 1024 / 1024).toFixed(1)} MB`;
+          progress({
+            stage: 'model',
+            progress: Math.min(0.65, Math.max(0.05, percentage / 100 * 0.65)),
+            message: `Downloading voice model (${size})...`,
+          });
+        },
+      });
+      this._config = config;
 
       // --- PUA compatibility check ------------------------------------------
       const puaCheck = checkPuaCompat(this._config.pua_compat_version);
@@ -624,8 +630,9 @@ export class PiperPlus {
         ort,
         gpu: undefined,
       });
-      this._modelUrl = modelUrl;
-      this._session = await this._sessionManager.createSession(modelUrl);
+      const modelBytes = new Uint8Array(modelData);
+      this._modelUrl = modelBytes;
+      this._session = await this._sessionManager.createSession(modelBytes);
 
       progress({ stage: 'model', progress: 0.7, message: 'Model loaded.' });
 
diff --git a/node_modules/piper-plus/src/model-manager.js b/node_modules/piper-plus/src/model-manager.js
index ed5fdc1..eee731b 100644
--- a/node_modules/piper-plus/src/model-manager.js
+++ b/node_modules/piper-plus/src/model-manager.js
@@ -158,13 +158,17 @@ async function resolveModelFiles(repoName) {
 export class ModelManager {
   /**
    * @param {Object} [options]
-   * @param {string} [options.cachePrefix='piper-plus-models'] - IndexedDB database name
-   * @param {boolean} [options.verifyOnCacheHit=false] - Re-verify SHA-256 on cache retrieval
+  * @param {string} [options.cachePrefix='piper-plus-models'] - IndexedDB database name
+  * @param {boolean} [options.verifyOnCacheHit=false] - Re-verify SHA-256 on cache retrieval
+   * @param {string} [options.expectedSha256] - Trusted SHA-256 for the selected model
+   * @param {number} [options.expectedByteLength] - Trusted byte length for the selected model
    */
   constructor(options = {}) {
     this._dbName = options.cachePrefix || DB_NAME;
     this._db = null;
     this._verifyOnCacheHit = options.verifyOnCacheHit || false;
+    this._expectedSha256 = options.expectedSha256 || null;
+    this._expectedByteLength = options.expectedByteLength || null;
   }
 
   /**
@@ -289,16 +293,25 @@ export class ModelManager {
     // Download model with progress tracking.
     const modelData = await fetchWithProgress(modelUrl, onProgress);
 
+    if (this._expectedByteLength && modelData.byteLength !== this._expectedByteLength) {
+      throw new Error(
+        `Downloaded model size mismatch: expected ${this._expectedByteLength}, got ${modelData.byteLength}`
+      );
+    }
+
     // Compute SHA-256 integrity hash.
     const sha256 = await this._computeSha256(modelData);
 
-    if (sha256 === null) {
+    if (sha256 === null && this._expectedSha256) {
+      throw new Error('The downloaded model could not be integrity-checked in this browser.');
+    } else if (sha256 === null) {
       console.warn('[piper-plus] crypto.subtle unavailable — skipping SHA-256 integrity verification. Serve over HTTPS for full integrity checks.');
-    } else if (config.sha256) {
+    } else if (this._expectedSha256 || config.sha256) {
       // Verify against the expected hash in the model config.
-      if (sha256 !== config.sha256) {
+      const expectedSha256 = this._expectedSha256 || config.sha256;
+      if (sha256 !== expectedSha256) {
         throw new Error(
-          `SHA-256 mismatch for downloaded model: expected ${config.sha256}, got ${sha256}`
+          `SHA-256 mismatch for downloaded model: expected ${expectedSha256}, got ${sha256}`
         );
       }
     }
@@ -336,15 +349,25 @@ export class ModelManager {
       return null;
     }
 
+    if (this._expectedByteLength && entry.modelData.byteLength !== this._expectedByteLength) {
+      return null;
+    }
+
     // Optionally verify integrity of cached data.
-    if (this._verifyOnCacheHit && entry.sha256) {
+    if (this._verifyOnCacheHit && (entry.sha256 || this._expectedSha256)) {
       const currentHash = await this._computeSha256(entry.modelData);
-      if (currentHash !== null && currentHash !== entry.sha256) {
+      if (currentHash === null && this._expectedSha256) {
+        return null;
+      }
+      if (currentHash !== null && entry.sha256 && currentHash !== entry.sha256) {
         console.warn(
           `[piper-plus] Cached model "${key}" failed SHA-256 integrity check (expected ${entry.sha256}, got ${currentHash}). Re-downloading.`
         );
         return null;
       }
+      if (currentHash !== null && this._expectedSha256 && currentHash !== this._expectedSha256) {
+        return null;
+      }
     }
 
     return { modelData: entry.modelData, config: entry.config };
diff --git a/node_modules/piper-plus/src/phonemizer/rust-wasm-adapter.js b/node_modules/piper-plus/src/phonemizer/rust-wasm-adapter.js
index 33bcff9..33ee2a4 100644
--- a/node_modules/piper-plus/src/phonemizer/rust-wasm-adapter.js
+++ b/node_modules/piper-plus/src/phonemizer/rust-wasm-adapter.js
@@ -39,18 +39,15 @@ export class RustWasmAdapter {
     if (options.wasmLoader) {
       wasmModule = await options.wasmLoader();
     } else {
-      const url = options.wasmUrl;
-      if (!url) throw new Error('Either wasmUrl or wasmLoader must be provided');
-      wasmModule = await import(url);
+      wasmModule = await import('../../dist/rust-wasm/piper_plus_wasm.js');
       await wasmModule.default(); // init() — load WASM binary
     }
     const wasm = new wasmModule.WasmPhonemizer(configJson);
 
     // Load Chinese pinyin dictionaries if setChineseDictionary is available
-    if (typeof wasm.setChineseDictionary === 'function') {
+    if (typeof wasm.setChineseDictionary === 'function' && options.zhDictBaseUrl) {
       try {
-        const dictBase = options.zhDictBaseUrl
-          || new URL('../../assets/', import.meta.url).href;
+        const dictBase = options.zhDictBaseUrl;
         const [singleResp, phraseResp] = await Promise.all([
           fetch(new URL('pinyin_single.json', dictBase)),
           fetch(new URL('pinyin_phrases.json', dictBase)),
diff --git a/node_modules/piper-plus/src/webgpu-session-manager.js b/node_modules/piper-plus/src/webgpu-session-manager.js
index 3aef7e6..6d27014 100644
--- a/node_modules/piper-plus/src/webgpu-session-manager.js
+++ b/node_modules/piper-plus/src/webgpu-session-manager.js
@@ -50,7 +50,7 @@ export class WebGPUSessionManager {
       try {
         const options = {
           executionProviders: [provider],
-          graphOptimizationLevel: 'extended',
+          graphOptimizationLevel: 'all',
           enableMemPattern: true,
         };
         const session = await this._ort.InferenceSession.create(modelPath, options);
diff --git a/node_modules/piper-plus/types/index.d.ts b/node_modules/piper-plus/types/index.d.ts
index d0f5691..176b844 100644
--- a/node_modules/piper-plus/types/index.d.ts
+++ b/node_modules/piper-plus/types/index.d.ts
@@ -75,6 +75,10 @@ export interface PiperPlusOptions {
   ort?: any;
   /** Progress callback invoked during initialization. */
   onProgress?: (info: ProgressInfo) => void;
+  /** Trusted SHA-256 digest for the pinned ONNX model. */
+  modelSha256?: string;
+  /** Trusted byte length for the pinned ONNX model. */
+  modelByteLength?: number;
 }
 
 /** Options for PiperPlus.synthesize(). */
